import React, {Component} from 'react';
import api from './api';

export default class sendAnythingPost extends Component{
    async sendingData(dataForm, urlPost){
        // const res = await api.post(urlPost, dataForm);
        return 0;
    }
}